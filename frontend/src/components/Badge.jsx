import React from 'react'

export default ({ children }) => (
    <div children={children} className="z-depth-1" style={{ background: 'rgb(255,152,101)', padding: '0.25em .5em', color: 'white' }} />
);